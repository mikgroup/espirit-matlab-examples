
To run the Matlab examples, set the environment variable 
TOOLBOX_PATH to the base directory of the reconstruction
tools. You can call the 'vars.m' matlab script from the
toolbox directory to set the path.


The Matlab examples demonstrate various parts of the
reconstruction toolbox. The 'bart' command is used call
the external tools. Results are then displayed in Matlab.




