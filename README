
To run the Matlab examples, set the environment variable 
TOOLBOX_PATH to the base directory of the reconstruction
tools E.g. if the toolbox is installed in ~/recon/ 
use the shell command 

export TOOLBOX_PATH=~/recon/

to set the environment variable.


FILES:


examples.m 		

Matlab examples demonstrating various parts of the 
reconstruction toolbox. The '!' command is used call the 
external tools. Results are then read into Matlab and 
displayed.




